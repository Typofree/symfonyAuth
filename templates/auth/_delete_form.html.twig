<form method="post" action="{{ path('app_auth_delete', {'id': auth.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ auth.id) }}">
    <button class="btn">Delete</button>
</form>
