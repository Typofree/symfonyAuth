{% extends 'base.html.twig' %}

{% block title %}Edit Auth{% endblock %}

{% block body %}
    <h1>Edit Auth</h1>

    {{ include('auth/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_auth_index') }}">back to list</a>

    {{ include('auth/_delete_form.html.twig') }}
{% endblock %}
